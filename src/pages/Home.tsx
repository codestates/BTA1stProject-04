import { Box } from '@mui/material';
import NavBar from '../components/NavBar';

const Home = () => {
  return (
    <Box sx={{ flexGrow: 1 }}>
      <NavBar />
      Hello World
    </Box>
  );
};

export default Home;
